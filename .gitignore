# Byte-compiled / optimized / DLL files
__pycache__/
*.py[cod]
*$py.class
*.egg-info/


# Environments
.env
venv/
.venv/          # [FIX F41] thiếu dòng này => .venv là untracked, `git clean -fd` xoá sạch
ai_env/
.pytest_cache/

# Artifacts & Heavy Data (NEVER commit)
/data/
models/weights/
models/rust_artifacts/
backtest/results/
scratch/
logs/
*.db
*.bin
*.onnx
*.lock
.aegis_trading.lock

# Cache dẫn xuất — SINH LẠI ĐƯỢC, không commit (4,2MB)
#   returns_v3*.csv   <- scripts/export_returns_v3.py
#   goal_policy.npz   <- scripts/build_goal_policy.py
artifacts/returns_v3*.csv
artifacts/returns_v3*.meta.json
artifacts/goal_policy.npz
